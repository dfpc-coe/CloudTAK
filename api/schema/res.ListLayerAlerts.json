{
    "type": "object",
    "required": [
        "total",
        "items"
    ],
    "additionalProperties": false,
    "properties": {
        "total": {
            "type": "integer",
            "description": "Total number of layers related to the query"
        },
        "items": {
            "type": "array",
            "description": "Array of alert objects",
            "items":  {
                "$ref": "layer_alerts.json"
            }
        }
    }
}
