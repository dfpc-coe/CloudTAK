{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "$ref": "./util/limit.json"
        },
        "page": {
            "$ref": "./util/page.json"
        },
        "order": {
            "$ref": "./util/order.json"
        },
        "sort": {
            "type": "string",
            "default": "created",
            "enum": [
                "id",
                "name",
                "created",
                "updated",
                "description",
                "enabled",
                "enabled_styles",
                "styles",
                "logging",
                "stale",
                "task",
                "connection",
                "cron",
                "environment",
                "memory",
                "timeout",
                "data",
                "schema"
            ],
            "description": "Field to sort order by"
        },
        "data": {
            "type": "integer",
            "description": "Limit returned layers to those attached to a given data sync"
        },
        "connection": {
            "type": "integer",
            "description": "Limit returned layers to those attached to a given connection"
        },
        "filter": {
            "type": "string",
            "default": "",
            "description": "Filter a complete or partial name"
        }
    }
}
