{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "$ref": "./util/limit.json"
        },
        "page": {
            "$ref": "./util/page.json"
        },
        "order": {
            "$ref": "./util/order.json"
        },
        "sort": {
            "type": "string",
            "default": "created",
            "enum": [
                "id",
                "created",
                "updated",
                "name",
                "description",
                "auto_transform",
                "connection"
            ],
            "description": "Field to sort order by"
        },
        "connection": {
            "type": "integer",
            "description": "Filter by a connection id"
        },
        "filter": {
            "type": "string",
            "default": "",
            "description": "Filter a complete or partial name"
        }
    }
}
