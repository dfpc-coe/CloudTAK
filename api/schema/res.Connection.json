{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "id": {
            "$ref": "./connections/id.json"
        },
        "status": {
            "type": "string",
            "enum": [
                "live",
                "dead",
                "unknown"
            ]
        },
        "certificate": {
            "type": "object",
            "properties": {
                "validTo": { "type": "string" },
                "validFrom": { "type": "string" }
            }
        },
        "name": {
            "$ref": "./connections/name.json"
        },
        "created": {
            "$ref": "./connections/created.json"
        },
        "updated": {
            "$ref": "./connections/updated.json"
        },
        "description": {
            "$ref": "./connections/description.json"
        },
        "enabled": {
            "$ref": "./connections/enabled.json"
        }
    },
    "required": [
        "id",
        "created",
        "updated",
        "name",
        "description",
        "enabled",
        "status"
    ]
}
