{
    "type": "object",
    "required": [
        "total",
        "status",
        "layers"
    ],
    "additionalProperties": false,
    "properties": {
        "total": {
            "type": "integer"
        },
        "status": {
            "type": "object",
            "required": [ "healthy", "alarm" , "unknown" ],
            "additionalProperties": false,
            "properties": {
                "healthy": { "type": "integer" },
                "alarm": { "type": "integer" },
                "unknown": { "type": "integer" }
            }
        },
        "layers": {
            "type": "array",
            "items":  {
                "$ref": "res.Layer.json"
            }
        }
    }
}
