{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "limit": {
            "$ref": "./util/limit.json"
        },
        "filter": {
            "type": "string",
            "description": "Filter assets by name"
        },
        "page": {
            "$ref": "./util/page.json"
        },
        "order": {
            "$ref": "./util/order.json"
        },
        "sort": {
            "type": "string",
            "default": "created",
            "enum": [
                "id",
                "name",
                "created",
                "updated",
                "storage"
            ],
            "description": "Field to sort order by"
        }
    }
}
