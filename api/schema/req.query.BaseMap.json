{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "download": {
            "type": "boolean",
            "description": "If true, set the Content-Disposition headers",
            "default": false
        },
        "format": {
            "type": "string",
            "default": "json",
            "description": "Format with which to respond",
            "enum": [
                "json",
                "xml"
            ]
        }
    }
}
