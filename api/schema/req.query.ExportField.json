{
    "type": "object",
    "properties": {
        "format": {
            "type": "string",
            "default": "csv",
            "description": "Format with which to respond",
            "enum": [
                "csv",
                "jsonld"
            ]
        }
    }
}
