import { createApp } from 'vue'
import App from './App.vue'

import '@tabler/core/dist/css/tabler.min.css'


const app = createApp(App)
app.mount('#app')
// app.use(Tabler) // Commenting out the use of Tabler since it's unused
